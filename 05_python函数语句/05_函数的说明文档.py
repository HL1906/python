
def add(x, y):
    """
    add函数可以接收两个参数，进行相加的功能
    :param x:形成x表示相加的其中一个数字
    :param y:形成y表示相加的另外一个数字
    :return:返回值两个数相加的结果
    """
    result = x + y
    print(f"{x} + {y} = {result}")
    return result


add(5, 6)
